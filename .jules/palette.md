## 2026-06-12 - Actionable Reset in Empty States
**Learning:** When users encounter a "No tasks found" empty state during a search or filter, failing to provide an immediate reset path creates a dead-end experience, requiring them to manually clear the search input.
**Action:** Always include an actionable reset button (like "Clear Search") in empty states for search or filter interfaces that links back to the base route without query parameters.
